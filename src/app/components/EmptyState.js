"use client";
// Simple placeholder shown when there is no data to display
function EmptyState({ message }) {
  return <div className="text-lg text-black">{message}</div>;
}

export default EmptyState;
