// "use client";
// import TaskCard from "./TaskCard";

// export default function TaskListRealtime({ initialItems = [] }) {
//   // const Tasks = useRealTimeListner(initialItems);
//   return (
//     <div className="space-y-6">
//       {Tasks.map((task) => {
//         return <TaskCard key={task.id} title={task.tittle} task={task} />;
//       })}
//     </div>
//   );
// }
